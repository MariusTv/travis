language: php

php:
  - 7.0

before_script:
  - composer self-update
  - cp .env.travis .env
  - composer install
  - mysql -e 'create database travis_test;'
  - php artisan key:generate
  - php artisan migrate

script: phpunit